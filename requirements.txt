pydantic>=2.6,<3.0
pdfplumber>=0.11,<1.0
python-docx>=1.1,<2.0
openpyxl>=3.1,<4.0
pytest>=8.0,<9.0
requests>=2.31,<3.0

# backend engineer: add fastapi, uvicorn, google-cloud-firestore,
# google-cloud-storage, google-cloud-pubsub, google-genai here as you wire
# each one up, so `pip install -r requirements.txt` always reflects what the
# app actually needs.
